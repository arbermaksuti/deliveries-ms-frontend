import { createTheme } from '@mui/material'
const theme = createTheme({
  palette: {
    primary: {
      main: '#f2dfb6',
    },
    secondary: {
      main: '#b58010',
    },
  },
})

export default theme
